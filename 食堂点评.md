# 食堂点评 Web App 设计文档

## 1. 项目概述

本项目是一个面向校园场景的食堂点评 Web App，第一阶段目标是用最小工程成本，先把移动端网页版体验做出来。

### 1.1 第一阶段目标

第一阶段做成一个 **纯前端演示版**，重点验证：

- 页面结构是否合理
- 导航流程是否顺畅
- 食堂、窗口、菜品、评价的交互是否自然
- 整体视觉风格是否符合预期
- 后续是否值得接入真实后端

### 1.2 第一阶段明确边界

第一阶段明确不做：

- 不写 `/backend` 项目
- 不接 PostgreSQL
- 不使用 Prisma ORM
- 不实现真实登录注册
- 不做图片上传
- 不做复杂权限系统
- 不做 AI 观察卡片
- 不做 AI 助手入口

### 1.3 长期方向

最终目标仍然是前后端分离，并通过 Capacitor 打包成手机 App。  
第二阶段再考虑接入 Express 后端、PostgreSQL、Prisma、用户登录、管理端等功能。

---

## 2. 第一阶段技术栈

### 2.1 必选技术栈

- React
- TypeScript
- Vite
- Tailwind CSS
- React Router
- Zustand
- localStorage

### 2.2 可选辅助库

如果实现时确实需要，可以使用以下库：

- `clsx`：组合条件 class
- `tailwind-merge`：合并 Tailwind class，避免样式冲突
- `Lucide React`：图标库
- `Framer Motion`：仅在需要轻微页面 / 卡片动效时使用，不是必选项

### 2.3 第一阶段不建议使用的技术

- Redux：本阶段状态规模不大，会增加模板代码
- React Query：本阶段没有真实后端 API
- Ant Design / Material UI：会冲淡参考小程序的漫画卡片风格
- Express / Prisma / PostgreSQL：放到第二阶段

---

## 3. 项目结构建议

第一阶段只创建前端项目，结构建议如下：

```
/frontend
  /public
    /images
  /src
    /assets
    /components
    /layouts
    /mock
    /pages
    /router
    /stores
    /styles
    /types
    /utils
    main.tsx
    App.tsx
```

说明：

- `/mock`：集中管理食堂、窗口、菜品、评价、用户等 mock 数据
- `/stores`：集中管理 Zustand store，例如用户评价、收藏、浏览记录
- `/layouts`：放置底部 Tab 导航等公共布局组件
- `/pages`：首页、排行榜、食堂列表、食堂详情、菜品详情、我的页面
- `/components`：按钮、卡片、标签、评分星星、评价列表、排行榜项等可复用组件
- `/router`：React Router 路由配置
- `/types`：Canteen、Window、Dish、Review、User 等类型定义
- `/utils`：格式化时间、计算评分、管理 localStorage 等工具函数

---

## 4. 页面与路由设计

第一阶段采用移动端优先布局，页面最大宽度约 `390px`，居中显示，模拟手机 App 体验。

### 4.1 路由结构

- `/`：首页
- `/ranking`：排行榜页
- `/canteens`：食堂列表页
- `/canteens/:canteenId`：食堂详情页
- `/dishes/:dishId`：菜品详情与评价页
- `/profile`：我的页面

### 4.2 底部 Tab

底部固定 4 个 Tab：

- 首页
- 排行
- 食堂
- 我的

### 4.3 页面说明

#### 4.3.1 首页

首页负责展示入口信息和快速导航，包含：

- 搜索框
- 快捷筛选标签
- 食堂数、窗口数、评价数等统计展示
- 菜品分类入口
- 今日推荐 / 热门菜品卡片
- 点击菜品可进入菜品详情

#### 4.3.2 排行榜页

排行榜页负责展示不同维度的菜品榜单，包含：

- 今日榜 / 本周榜切换
- 人气 / 出餐快 / 省钱分类切换
- 前三名突出展示
- 完整排行列表
- 列表项点击后进入菜品详情

#### 4.3.3 食堂列表页

食堂列表页负责展示所有食堂，包含：

- 食堂卡片列表
- 名称、楼层、窗口数量、营业状态
- 点击后进入食堂详情

#### 4.3.4 食堂详情页

食堂详情页负责展示某个食堂内部结构，包含：

- 食堂基础信息
- 窗口列表
- 每个窗口下的菜品列表
- 点击菜品进入菜品详情

#### 4.3.5 菜品详情与评价页

菜品详情页是第一阶段的核心交互页面，包含：

- 菜品名称、价格、评分
- 所属食堂与窗口信息
- 历史评价列表
- 1-5 星评分输入
- 文字评价输入
- 提交评价功能
- 提交后立即显示在页面，并写入 `localStorage`

#### 4.3.6 我的页面

我的页面负责展示用户相关内容，包含：

- mock 用户信息卡片
- 数据统计栏
- 常用入口四宫格
- 我的评价、收藏、浏览记录等列表入口
- 不包含 AI 助手入口

---

## 5. 数据模型与 Mock 数据

第一阶段所有数据都来自前端本地 mock 文件，用户提交的新内容写入浏览器 `localStorage`，并在展示时合并到对应列表中。

### 5.1 Canteen 食堂

字段建议：

- `id`
- `name`
- `description`
- `floors`
- `windowCount`
- `status`：`open` / `closed`
- `tags`
- `location`
- `cover`
- `rating`

### 5.2 Window 窗口

字段建议：

- `id`
- `canteenId`
- `name`
- `floor`
- `description`
- `status`
- `tags`
- `dishIds`

### 5.3 Dish 菜品

字段建议：

- `id`
- `windowId`
- `canteenId`
- `name`
- `category`
- `price`
- `description`
- `image`
- `rating`
- `reviewCount`
- `speedScore`
- `valueScore`
- `popularity`
- `tags`

### 5.4 Review 评价

字段建议：

- `id`
- `dishId`
- `userId`
- `userName`
- `avatar`
- `rating`
- `content`
- `createdAt`
- `likes`

### 5.5 User 用户

字段建议：

- `id`
- `nickname`
- `avatar`
- `bio`
- `stats`

### 5.6 Mock 数据要求

至少提供：

- 3 个食堂：一餐厅、二餐厅、民族餐厅
- 每个食堂 3-4 个窗口
- 每个窗口 4-6 个菜品
- 每个菜品若干条历史评价
- 菜品分类至少覆盖：热菜、凉菜、面食、小吃、水果、饮品
- 排行榜数据优先从现有菜品与评价数据派生，不单独维护冗余榜单数据

---

## 6. 图片与素材策略

第一阶段优先复用 `C:\Users\lenovo\Desktop\Canteen` 小程序项目中的已有图片素材，用于保持视觉风格一致。

使用原则：

- 优先复用本地已有素材
- 如果某些菜品缺少图片，使用同类菜品占位图、首字母图形、emoji 或统一风格色块卡片
- 图片统一放于 `/frontend/src/assets/` 或 `/frontend/public/images/`
- 不要求第一阶段每个菜品都有真实照片
- 先保证页面完整可体验，图片素材可后续逐步替换

---

## 7. UI 风格规范

第一阶段采用 **网页轻量复刻版**，视觉风格以 `C:\Users\lenovo\Desktop\Canteen` 小程序项目为主要参考。

### 7.1 核心风格

- 移动端优先，最大宽度约 `390px`，居中显示
- 页面背景为浅米色 / 浅灰色
- 主要文字色为深墨蓝 `#172033`
- 以白色或米白色卡片为主
- 使用粗黑描边和轻微错位阴影
- 使用彩色胶囊标签和贴纸感小元素
- 保留少量波点、贴纸、彩色角标
- 不过度堆复杂漫画装饰，保证 React / Tailwind 实现简单

### 7.2 参考颜色

- 墨蓝：`#172033`
- 背景灰：`#F7F8FA`
- 米白：`#FFFDF7`
- 浅橙：`#FFF7E8`
- 蓝色：`#1677FF`
- 绿色：`#22A06B`
- 琥珀黄：`#FFB020`
- 红色：`#FF4D4F`
- 粉色：`#FF4FB8`
- 青色：`#00C2FF`
- 亮黄：`#FFE14D`

### 7.3 组件风格建议

- 卡片：圆角较大，带 2-3px 深色描边，以及 4-8px 彩色或灰色错位阴影
- 按钮：深色底、白字、彩色阴影
- 标签：胶囊状、浅色底、深色描边
- 底部 Tab：白色底、深色描边、大圆角，选中项有浅橙背景和阴影
- 标题：加粗，可使用轻微黄色文字阴影
- 列表：卡片式为主，不做传统表格
- 交互：点击时可轻微缩放

### 7.4 视觉对齐方式

实现时应优先读取并参考以下小程序样式文件，尽量对齐视觉气质：

- `C:\Users\lenovo\Desktop\Canteen\app.wxss`
- `C:\Users\lenovo\Desktop\Canteen\custom-tab-bar\index.wxss`
- `C:\Users\lenovo\Desktop\Canteen\pages\canteens\canteens.wxss`
- `C:\Users\lenovo\Desktop\Canteen\pages\ranking\ranking.wxss`
- `C:\Users\lenovo\Desktop\Canteen\pages\profile\profile.wxss`

目标是做到“风格接近、细节简化、适合网页实现”，而不是逐像素复制。

---

## 8. 状态管理与本地持久化

### 8.1 状态管理

推荐使用 Zustand 管理以下状态：

- 用户提交的新评价
- 收藏记录
- 浏览记录
- 当前排行榜 Tab / 分类选择
- 当前筛选条件
- 我的页面展示数据

### 8.2 本地持久化

以下内容写入 `localStorage`：

- 用户提交的评价
- 收藏记录
- 浏览记录

### 8.3 固定数据

以下内容可固定写在代码中：

- mock 用户信息
- 默认食堂、窗口、菜品、历史评价

---

## 9. 验收标准

第一阶段完成后，应能在浏览器里完成以下完整体验：

### 9.1 首页

- 能看到搜索框、筛选标签、统计数据、分类入口、推荐菜品
- 点击推荐菜品能进入菜品详情

### 9.2 排行榜

- 能切换今日榜 / 本周榜
- 能切换人气 / 出餐快 / 省钱
- 前三名展示突出
- 列表项能进入菜品详情

### 9.3 食堂页

- 能看到至少 3 个食堂
- 点击食堂进入详情
- 详情页能看到窗口和菜品

### 9.4 评价流程

- 菜品详情页能展示历史评价
- 用户能选择 1-5 星
- 用户能输入文字评价
- 提交后评价立即显示
- 刷新页面后新评价仍然存在

### 9.5 我的页面

- 能展示 mock 用户信息
- 能展示收藏、浏览记录、我的评价等内容入口
- 能体现用户在本地提交过的内容

### 9.6 UI 标准

- 页面移动端优先
- 最大宽度约 `390px`
- 风格接近 `C:\Users\lenovo\Desktop\Canteen` 小程序
- 底部 Tab 切换正常
- 各页面卡片、按钮、标签风格统一

---

## 10. 第二阶段规划（暂不实现）

第二阶段再考虑：

- 创建 `/backend`
- Express + TypeScript
- RESTful API
- Prisma schema
- PostgreSQL
- 前后端联调
- 用户登录注册
- 图片上传
- 管理后台
- Capacitor 打包 App
